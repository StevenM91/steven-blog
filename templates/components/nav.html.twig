<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_home') }}">My blog</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Menu</a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            {# Si un utilisateur est connecter (donc la super global app.user est remplis) alors on rentre dans la condition #}
            {% if app.user %}
              {# block conditionnelle peut servire à faire des conditions, boucle ect... #}
              <li>
                <a class="dropdown-item" href="{{ path('app_logout') }}">Deconnexion</a>
              </li>
            {% else %}
              <li>
                <a class="dropdown-item" href="{{ path('app_register') }}">Inscription</a>
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('app_login') }}">connexion</a>
              </li>
            {% endif %}

            {# {% if app.user and app.user.roles == ['ROLE_ADMIN', 'ROLE_USER'] %} // autre maniere pour le faire et definir les admin #}
            {# is_granted(ROLE_ADMIN) sert a voir si l'utilisateur est connecter et à le role admin #}
            {# app.user contien l'utilisateur connecter est biensur les information de celui ci #}
            {% if is_granted('ROLE_ADMIN') %}
              <li>
                <hr class="dropdown-divider" />
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('app_dashboard') }}">Back-Office</a>
              </li>
            {% endif %}
          </ul>
        </li>
      </ul>
      <div class="d-flex">
        {% if app.user %}
          <a href="{{ path('app_profil') }}"><strong class="text-white">Profil: {{ app.user.lastname }} {{ app.user.firstname }}</strong></a>
        {% endif %}
      </div>
    </div>
  </div>
</nav>
